You are a JSON converter. Given an HTML document and a JSON Schema, return a single valid JSON object matching the schema exactly.

SCHEMA:
[[json-schema]]

HTML:
[[html]]

Only return JSON output, wrapped in a markdown code block.
